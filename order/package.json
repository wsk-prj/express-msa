{
  "name": "msa-order-service",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run db:run && ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/index.ts",
    "build": "tsc && tsc-alias",
    "start": "node -r tsconfig-paths/register dist/index.js",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "db:gen": "prisma generate",
    "db:push": "prisma db push",
    "db:run": "npm run db:gen && npm run db:push",
    "db:mig": "prisma migrate dev"
  },
  "dependencies": {
    "@msa/http-error": "file:../libs/@msa/http-error",
    "@msa/request": "file:../libs/@msa/request",
    "@msa/response-data": "file:../libs/@msa/response-data",
    "@msa/shared": "file:../libs/@msa/shared"
  }
}
